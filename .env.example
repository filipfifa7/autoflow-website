# Copy this file to .env.local and fill in your details.
# Contact form sends to info@autoflow.hr. These values are used ONLY on the server (never in the browser).

# Zoho Mail (info@autoflow.hr)
# Custom domain: smtppro.zoho.com (paid) or smtp.zoho.com
# Port 587 = TLS, Port 465 = SSL
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_USER=info@autoflow.hr
SMTP_PASS=your-zoho-password
SMTP_FROM=info@autoflow.hr
